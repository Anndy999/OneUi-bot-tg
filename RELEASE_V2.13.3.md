# OneUI Firmware Worker v2.13.3

## Administrator command help

The following command is now documented in both the administrator quick guide
and full administrator help:

/moninterval 15

It sets one default monitoring interval for all default monitor targets. Valid
values are 1-1440 minutes; existing per-device interval overrides remain in
place.
