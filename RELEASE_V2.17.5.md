# OneUI Firmware Worker v2.17.5

## 版本说明

这是 OneUI 机器人稳定 VPS 版本的运行基线，包含以下核心能力：

- Telegram 长轮询运行，不依赖 Webhook、域名或 Nginx。
- PostgreSQL 保存持久化配置，Redis 和 BullMQ 负责队列与任务调度。
- 三星官方固件查询、CSC 建议、监控和新版本通知。
- 管理员专用固件下载，支持暂停、继续、删除和下载进度查询。
- 下载文件保存在 VPS 指定目录，并通过 OpenList 提取。
- systemd 开机自启动、健康检查、日志查看和数据库迁移脚本。
- 发布链支持 S26/S25 地区阶段切换与管理员确认。

## 通知规则

普通监控只在发现新版本时通知；查询失败、三星暂时无数据和重试过程不会向普通用户发送自动通知。

## 升级方式

VPS 通过 `deploy/update-vps.sh` 从 `origin/main` 快进更新，不覆盖用户配置、生产数据库或下载文件。
