# OneUI Firmware Worker v2.10.0

本版本增加“三星官方 CSC 智能纠错”，面向手机、平板、手表、耳机与戒指的统一查询流程。

## 新功能

- 用户输入的 Model 格式合法，但指定 CSC 没有精确官方记录时，Worker 会读取同一次 Samsung FUS SmartHistory 响应中的其他可用地区记录。
- 仅保留满足以下条件的候选：
  - `BINARY_MODEL_NAME` 与请求型号精确一致；
  - 有可用正式版本；
  - 未被 `BINARY_EXIST=0` 等字段标记为撤回；
  - 公开日期不在未来；
  - CSC 来自三星返回的 Local CSC 或 Buyer CSC。
- 默认按以下信号综合排序：
  - 与用户输入 CSC 的地区相似度；
  - 中文或英文使用环境的地区优先级；
  - 型号后缀体现的地区系列；
  - 官方记录的公开日期与 Sequence。
- 首屏显示最可能的两个官方 CSC，并显示地区、最新 PDA 与公开日期。
- 点击“查看更多官方 CSC”可分页浏览本次官方响应中全部有效候选。
- 点击建议 CSC 可直接查询，不会因为纠正同一次错误而再次扣除白名单用户的 Model 日额度，但仍受短时防刷限制。

## 权威性与安全边界

- 其他 CSC 的记录只用于纠错建议，绝不会成为原请求 CSC 的正式结果。
- 固件正式结果仍要求精确 Local CSC 或 Buyer CSC。
- 不接入第三方固件数据库，不使用 `version.xml`，不展示未经三星 SmartHistory 响应确认的动态候选。
- 已确认的少量静态纠错仅作为三星响应未返回候选时的兜底。
- 任意合法的 `SM-*` 或 `SC-*` 移动设备型号都可进入该机制；若型号本身拼写错误且三星接口没有返回任何可识别型号，仍会返回普通查询失败，而不会猜测一个未经官方响应确认的型号。

## 部署兼容性

- 可直接覆盖 v2.9.2。
- 不新增 Worker Secret、KV Namespace、Durable Object 类、Migration 或 Queue。
- 不改变现有监控目标、调度间隔、通知队列、定时恢复与 KV 配额保护。
- 保留现有中英文管理员界面和 SmartHistory Android 缺失值编码保护。
