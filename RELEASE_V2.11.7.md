# v2.11.7 - Monitoring pause shortcut

`/monsnooze` is now included in Telegram's command menu. The command remains
administrator-only and accepts a model, CSC, and duration such as:

`/monsnooze 9380 TGY 6h`
