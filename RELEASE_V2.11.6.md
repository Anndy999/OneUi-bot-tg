# v2.11.6 - Admin command shortcut

The compact Telegram command menu now includes `/admin`.

The command remains permission protected: only the configured administrator
opens the management panel; every other user receives the existing no-access
reply.
